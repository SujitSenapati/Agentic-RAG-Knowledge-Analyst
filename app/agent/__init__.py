"""
`app.agent` package.

Contains the agent orchestration components: planner, reasoner,
critic, judge, and the main execution loop. These modules implement
the staged agent pipeline: plan -> retrieve -> reason -> critique -> judge.
"""
