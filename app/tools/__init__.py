"""
`app.tools` package.

Provides a small ecosystem for tool registration and retrieval-based
helpers. Tools are registered via the `TOOL_REGISTRY` and used by the
planner to select appropriate retrieval functions.
"""
